name: zello_shared
description: "Shared models, API client, and theme for Zello Saúde apps."
version: 0.0.1

environment:
  sdk: ^3.7.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  intl: ^0.19.0
  flutter_riverpod: ^2.6.0
  supabase_flutter: ^2.8.4
  encrypt: ^5.0.3
  google_fonts: ^6.2.0
  flutter_local_notifications: ^18.0.1
  timezone: ^0.9.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
